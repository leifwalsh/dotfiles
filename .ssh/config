Host *
ForwardAgent yes                    # forward ssh-agent auth stuff
ControlMaster auto                  # multiplex connections to one host
ControlPath ~/.ssh/master-%r@%h:%p  # where is that file
Compression yes                     # use compression
CompressionLevel 9                  # really really compress it
ForwardX11 yes                      # forward X stuff
ForwardX11Trusted yes               # yes, really forward X stuff
ServerAliveInterval 60              # don't drop my connections
ServerAliveCountMax 1440            # for one day
